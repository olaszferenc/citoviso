# Distill review — 20260906T020001Z

Verification complete. `shrinkForVision` is gated on `raw.length > MAX_INLINE_BYTES` (3 MB) — the memory's claim is accurate, no drift. I now have everything I need to produce the review.

# Distill review

## SUMMARY
31 új/módosult epizodikus memória (sok közülük ugyanannak a szálnak a session-jegyzet + tömör memória-párja). Eredmény: **8 PROMOTE-blokk** (egy koherens új domain-terület: egyedi-domain provisioning + kötbér-modell → glossary + calc-model + új §K; továbbá 4 §B.17/§C invariáns-bővítés + 1 entity-map gotcha), **1 REFINE** (§D.10 deploy-invariáns elavult az ADR-0053 óta), a többség **SKIP** (már fedve vagy epizodikus zaj/tooling), **0 DRIFT** (a kockázatos grounding-kicsinyítés állítást a kódban ellenőriztem — pontos). Kód-ellenőrzés: `domainSettlement.ts`, `images.ts`, `photoQuality.ts`, `multilangOrder.ts`, `sendBatch.ts` — mind alátámasztja a promotált szabályokat.

---

## PROMOTE  (új ontológia-bejegyzések)

### 00-GLOSSARY.md → Üzleti fogalmak
Source: project_domain_automation.md (+ 2026-09-03_domain_terms_adr0093_0094.md, 2026-09-04_domain_settlement_impl_adr0094.md)
```append
- **Egyedi-domain provisioning** — a lead/tenant saját domainjének automata beszerzése + bekötése. A **FIZETÉS a trigger** (nincs operátori jóváhagyás: a vétel maga a megrendelt szolgáltatás); a biztonságot a registrar **atomi check-and-register** adja (ha időközben elkelt, a vétel elhasal, nem veszünk rosszat). Állapotgép: `pending→registering→registered→dns_pending→tls_pending→live` (+`failed`), idempotens; a `site.custom_domain` CSAK `live`-nál íródik be. Kód: `src/domains/provisionDomain.ts`, adapterek env-kapcsolóval (`REGISTRAR_PROVIDER`/`DNS_PROVIDER`, lokál alapból `mock`). Lásd ADR-0071/0078, [01-CALC-MODELS] kötbér, [03-INVARIANTS] §K.
- **Hűségidő (elköteleződés) — KÖTBÉR-modell, NEM szabad lemondás** — az ingyen/kedvezményes domainhez vállalt minimum-időszak. Alatta **nincs rendes felmondás**; korai kilépés = a hátralévő hónapok díja a VÁLLALT MINIMUM tarifán (kötbér) + a domain vételára CSAK ha elviszi. A **domain a ZÁLOG**: átszállás csak maradéktalan rendezés után (önvégrehajtó behajtás + előfizetés-freeze). ⛔ A megtartási/kedvezmény-ajánlat NEM pótolja a hiányzó kötelmet — szankció nélkül a felület csak ellentmondást szül. Lásd ADR-0094, [03-INVARIANTS] §K.
- **Csomag-padló (`committed_min_monthly`)** — a hűségidő alatt tartandó minimum havidíj, ami **rendeléskor BEFAGY** az `order_intent`-en (ingyen-domain rendelésnél = az ingyen-küszöb). Közös olvasó: `activeDomainCommitment()` (`src/domains/domainCommitment.ts`); a modulváltás (`applyModuleChange`) ATOMIAN elutasítja a padló alá csökkentést. Lásd ADR-0094 ④.
```

### 01-CALC-MODELS.md → Unit economics (per tenant) alá, új szekció
Source: 2026-09-04_domain_settlement_impl_adr0094.md (+ project_domain_automation.md)
```append
## Domain-kötbér (korai kilépés elszámolása)
- **Kötbér-alap (havi):** `floorMonthly ?? computeMonthly(renewableModuleIds)` — a rendeléskor befagyott csomag-padló, vagy annak hiányában a megújítható modulok aktuális havidíja (`src/domains/domainSettlement.ts`).
- **Kötbér-összeg:** `remainingMonths × kötbér-alap` — MINDIG jár (nem opcionális).
- **Domain vételára:** hozzáadódik CSAK ha a tenant elviszi a domaint (a tenant dönt; kiléphet domain nélkül is).
- **Végösszeg** = kötbér-összeg + (viszi ? domain-vételár : 0). Egyetlen olvasó (`settlementQuote()`) eteti a GET-lapot, a POST-ordert és a teszteket — a tenant sosem fizet mást, mint amit látott.
- **Paraméterek (HU pricing-lap, EGY közös érték):** ár-plafon 15 €/év · min. elköteleződés 12 hó · ingyen-küszöb 8000 Ft/hó · domain-vételár 20 000 Ft. Lásd ADR-0093/0094.
```

### 03-INVARIANTS.md → új szakasz a §J után: §K — Egyedi domain & elköteleződés
Source: project_domain_automation.md (verifikálva: `src/domains/domainCommitment.ts:42`, `domainSettlement.ts:5,51`, `src/payment/service.ts:251,262`)
```append
## §K — Egyedi domain & elköteleződés (ADR-0071/0078/0093/0094)
27. **A FIZETÉS a domain-beszerzés triggere — nincs operátori jóváhagyás.** A `handleWebhook` `paid` ága indítja a provisioninget (`kind='domain_upgrade'`/`'domain_settlement'`). A biztonság a registrar atomi check-and-register-jéből jön, nem emberi kapuból. A live adapterek **őszinte stubok**: kulcs nélkül a konstruktorban dobnak (nincs néma mock-fallback).
28. **A domain a ZÁLOG — átszállás csak maradéktalan rendezés után.** Hűségidő alatt nincs rendes felmondás; a korai kilépés kötbért von maga után ([01-CALC-MODELS] domain-kötbér). A behajtás önvégrehajtó (a domain visszatartása + előfizetés-freeze), NEM külön jogi lépés.
29. **Nem ígérünk visszautalást, amit nem építettünk meg.** Sikertelen beszerzésnél nincs auto-refund: a tenant másik nevet választ, arra fordítjuk az összeget. A felület ezért NEM ígér visszautalást (§B.17 magunkra is áll: `feedback_missing_data_branch`/§B.17 szellemében nem állítunk olyan képességet, ami nincs). Indok: nem képtelenség (a Barionnak van Refund API-ja), hanem meg-nem-épített funkció.
30. **Az ár-plafon őr fail-closed.** A `RegistrarAdapter.getYearlyPriceEur` plafon feletti (vagy stub-dobó) ára → ajánlat-szűrés + kemény kapu a `register()` előtt; plafon felett `failed`, pénz nem mozog.
```

### 03-INVARIANTS.md → §B.17 (Tényhűség) enforce-kontraktus alá
Source: project_ai_cost_meter.md / 2026-08-30_ai_cost_meter_and_fact_gate.md (verifikálva: `src/generator/images.ts:34,130` — a shrink CSAK `>MAX_INLINE_BYTES` (3 MB) ágon fut)
```append
   - **A grounding-fotó FELBONTÁSA tényhűség-tengely, nem költség-tengely.** A modell HARD tényt olvas a képről (§B.17.b image-grounded), és a felbontás csökkentése MÉRTEN fabrikációt/kihagyást okoz: ugyanaz a „légkondicionált" állítás teljes felbontáson 3/3 helyes, 1024px-en 0/3 (+egyszer „ventilátoros szobák" fabrikálva). Ezért a grounding-pixelek maradnak; kicsinyítés CSAK akkor indokolt, ha az alternatíva a fotó teljes ELEJTÉSE (>3 MB inline-korlát → nyers URL = Cloudflare-blokk = elveszett grounding). „§B.17 > költség." Forrás: ADR-0085, `src/generator/images.ts`.
   - **Cross-site kép = idegen kreatív, nem a szállás fotója — ELVETENDŐ.** A listázó oldal HOSZTJÁTÓL eltérő domainen tárolt kép (reklám-banner, kampány-kreatív) NEM lehet a mock fotója/heroja, MÉG vouch-olt/hitelesített adatlapról sem (mért eset: `balaton.hu`-hosztolt Mirabella-camping banner egy másik szállás adatlapján). Igazságforrás a listázó host; a hoszt-lista átmeneti réteg, tartós helye a platform-registry. Forrás: ADR-0096 ④, `src/scraper/sources/portals/photoQuality.ts`.
   - **Az adathiányos ág NE legyen csendes átengedés.** Minden őrnél, aminek van „ha van adat / ha nincs adat" ága: a nincs-adat ág a VAK ág, épp ahol a kimenet a legrosszabb (kevesebb tény = több hely a tölteléknek). Tedd fel legalább a leggyengébb kérdést (nevez-e meg BÁRMIT, ami a felhasználónak érték?), és bukj rá; az INDOKLÁS mutasson az adathiányra, de a verdikt akkor is bukás. Kié a hiba (scrape vs szövegírás), az nem változtat azon, hogy kimehet-e. Kapcsolódó enforce: az `factVerdict="error"` is BLOKKOLJA a küldést (mail+SMS) — ellenőrizetlen ≠ küldhető (`src/outreach/sendBatch.ts`, `sendOutreachSms.ts`). Forrás: feedback_missing_data_branch_is_the_blind_branch, ADR-0085.
```

### 03-INVARIANTS.md → §C (Outreach) enforce-kontraktus alá
Source: reference_mms_send_gsm_modem.md / 2026-08-29_channel_gates_and_live_sms.md (ADR-0082/0083)
```append
   - **Csatorna-mátrix — a §C.8 kötelező elemei a PÁRRA együtt állnak.** A hideg mobil-megkeresés = **MMS (kép, wow) + kísérő SMS**. Az MMS fizikailag csak képet + ASCII-tárgyat visz, ezért a §C.8 jogi kötelezői (egykattintásos leiratkozó-link, azonosítható feladó, jogalap, személyre szabás) a **kísérő SMS-ben** teljesülnek; a jog/provenance-őr a PÁRT együtt ítéli. `mms_sent_at` sms_sent_at nélkül = megszakadt pár (a lead látta a képet, a jogi kísérő nem ment ki). Önálló hideg SMS a felületről KIVEZETVE. ⚠️ A transport-„siker" ≠ ép kézbesítés (mért: `gammu-smsd-inject` `-len` nélkül 70 karakternél némán csonkít). Forrás: ADR-0082/0083.
   - **Ismétlődő kártyás megbízás = pecsételt hozzájárulás + visszavonhatóság.** Automata, ismétlődő kártyaterhelés (MIT) CSAK explicit, PECSÉTELT vevő-hozzájárulással indítható (ÁSZF-verzió + checkout-hozzájárulás rögzítve, `recurring_consent_at`/`recurring_consent_text`), és a vevőnek elérhető **kétlépéses visszavonás** kell, ami a tokent **TÖRLI** (megtartott token = későbbi kód újra terhelhetne). ⛔ Egykattintásos „bekapcsolás" NEM adható: a kártyaséma a tárolt hitelesítőt 3DS-kihívott, VEVŐ-INDÍTOTT fizetéshez köti — a megbízást a következő fizetési link kiegyenlítése adja újra. Élesítés-kötés: az ÁSZF-szöveg és az admin-visszavonás EGYÜTT megy ki. Forrás: ADR-0088 ⑨, `src/legal.ts:59` (RECURRING_MANDATE_V1), `src/payment/subscription.ts` (revokeAutoCharge).
```

### 02-ENTITY-MAP.md → új szekció a „Tervezett entitások" előtt
Source: reference_document_storage_split.md, 2026-08-29_channel_gates_and_live_sms.md, 2026-08-28_multilang_purchase_defects.md
```append
## Rendelés/számla/outreach — adatmodell-gotchák (a kód igazsága)
- **`order_intent.kind`** ∈ `initial | renewal | upsell | multilang | domain_upgrade | domain_settlement`. Minden `kind`-nek külön `handleWebhook` `paid`-ága van (fulfillment). A **multilang** rendelés a vevő-azonosságot az `initial` rendelésről ÖRÖKLI, **fail-closed**: honnan-örökölni-hiánynál NINCS pay-link (számlázhatatlan pénzt nem veszünk el — 0029-kapu). Forrás: `src/tenant/multilangOrder.ts:57`.
- **`invoice`-on NINCS `tenant_id`.** A tenant-hoz vezető út: `invoice → payment.order_intent_id → order_intent.prospect_id → prospect.lead_id = tenant.lead_id`. Joinolható, de nem triviális — a tenant-admin számlalistához ez kell.
- **Prospect outreach-pecsétek — csatornánként külön.** `sent_at` = az ELSŐ ÉRINTÉS (H1-funnel bázis), plusz `email_sent_at` / `sms_sent_at` / `mms_sent_at` csatornánként. ⛔ A közös `sent_at`-ot csatorna-kapuként használni HIBA (az egyik csatorna elzárja a másikat — mért, ADR-0082).
- **Bizonylat FIZIKAI helye — egy szabály, két ág:** amit MI generálunk (kimenő számla PDF), kicsi és kevés → **DB** (`invoice.pdf_base64`, 0030); amit FELTÖLTENEK (szkennelt bejövő bizonylat), nagy és tetszőleges mennyiségű → **fájl** (`accounting_document.document_file`, `sites/_documents/`, 0031). Küszöb: ~1 GB fölött a kimenő ág is fájlba költözik. Forrás: ADR-0086 ②.
```

---

## REFINE  (meglévő szerkesztések — figyelmesen)

### 03-INVARIANTS.md → §D (Deploy) 10. pont
Source: reference_prod_version_deploy.md (ADR-0053, MEMORY.md) + CLAUDE.md §0.2 + reference_citoviso_prod_infra.md (ADR-0024, éles infra ÉL 2026-08-02 óta)
- OLD: `10. Lokál-először; élesre CSAK módosított fájlok, push-onként ÚJ scope-olt engedéllyel. Éles cél amúgy még TBD.`
- NEW: `10. Lokál-először; élesre **VERZIÓ megy, nem fájl-másolat (ADR-0053)** — egy MEGNEVEZETT COMMIT kicsekkolása (`scripts/deploy-prod.sh <commit> --go`, csak origin/main-en lévő commit), push-onként ÚJ scope-olt engedéllyel. ⛔ A korábbi „csak a módosított fájlokat visszük" FELÜLÍRVA (az termelte a 8 dátumból összeálló, sehol nem tesztelt éles kollázst). Éles cél ÉL: Hetzner CX23/Debian13, `/opt/citoviso/app`, `citoviso-public`(:4800)/`citoviso-console`(:4600) (ADR-0024, 2026-08-02 óta).`
- WHY: A §D.10 két állítása is elavult — a „CSAK módosított fájlok" a CLAUDE.md §0.2 által EXPLICITEN felülírt szabály, és az „éles cél TBD" hamis (a prod infra 2026-08-02 óta él). A stale invariáns pont a legkockázatosabb (deploy). ⚠️ Ember döntse el: a §0 deploy-doktrína a CLAUDE.md-ben a kanonikus — a §D csak a tükre; érdemes lehet §D-t a CLAUDE.md §0-ra mutató egysoros hivatkozásra karcsúsítani a duplikáció-drift ellen.

---

## SKIP  (már fedve — audit)
- Offer/listaár/ajánlat-réteg (kedvezmény sosem halmozódik, sent_at-ból származtatott jogosultság) — már itt: 00-GLOSSARY „Listaár" + „Ajánlat (offer)". (project_offer_and_recurring_mandate — csak az ismétlődő-mandátum RÉSZE promotálva.)
- KB-doktrína ①–⑤, audience-bontott őr, screenshot-frissesség, deploy-kapu (ADR-0045/*) — már itt: 03-INVARIANTS §J.24–26 + 00-GLOSSARY „Tudásbázis (KB)"/„KB-anchor". (project_knowledge_base_doctrine, project_kb_freshness_loop, 2026-09-*_operator_console_kb / kb_deploy_gate — tooling/impl-részletek.)
- „Gombfelirat CSAK idézett-félkövérrel" — már implicit itt: §J.24 enforce-sora (`**„label”**`-drift). (Nem külön invariáns; a jelölés-konvenció a kapu része.)
- Modul = a template NATÍV szekciójába befolyó adat, nem hozzáfűzött blokk; „ál-választás" (toggle, ami semmit nem változtat) tilos — már fedve: feedback_modules_weave_not_append + §B.17 fázis-határ (ADR-0015 minta-modul) + §A.1 „nincs üres oldal". (2026-08-31_module_shop_preview, ADR-0089.)
- Nincs kép nélküli oldal / demó-kép marad ha nincs owner-feltöltés — már itt: §A.1 következmény + §I.23. (module_shop_preview galéria-kapcsoló.)
- Tényhűség: főcím nevezzen meg konkrétumot, ne hordjon építőanyagot; leírás=tény-forrás de számot ne vegyünk át — a §B.17 HARD/SOFT kontraktus alá tartozik (soft: hangulat; hard: szám/díj). A konkrét főcím-mérce impl-részlet. (2026-09-01_mock_copy_sells.)
- Ígéret⇔szállítás, bait-and-switch tilalom — már itt: §I.23 + invariant_no_bait_and_switch_delivery.
- Elek gépi tesztelő, proof-of-work screenshot, /test-log — tooling/munkamód, nem domain-ontológia. (project_elek_test_agent, feedback_findings_always_with_screenshots, 2026-09-04/05 Elek-jegyzetek.)
- Booking-modul „lukak" figyelmeztetés — átmeneti projekt-warning, a Booking mint tervezett entitás már az ENTITY-MAP-ban; a warning maga epizodikus (a tisztázó kör zárultáig él). (project_booking_module_gaps.)
- Prod-mentés (PULL-modell, önellenőrzés), dev-DB közös/nincs mentés, migráció-névütközés, symlink-tilalom, C-kolláció ILIKE-csapda — infra/tooling reference-ek, nem domain-invariánsok. (reference_prod_backup, reference_shared_dev_db_has_no_backup.)
- Hero-kontraszt őr (WCAG ≥3,0), masthead-kontraktus, méret-infláció≠dizájn, nyelvváltó-láthatóság őr — dizájn-impl/munkamód; a „mérd, amit a látogató lát" elv már fedve: feedback_guard_must_measure_what_matters. (2026-09-01_hero_contrast, 2026-08-30_masthead, feedback_size_inflation_is_not_design, 2026-08-28_multilang ③.)
- Doktrína-a-munkafával-fagy (ADR-0079), nested-form néma bukás, prompt-a-kapu-előtt, csonka-SMS `-len` — engineering/testing-doktrína; már memóriákban él (feedback_doctrine_freezes…, feedback_invalid_markup…, feedback_check_the_prompt…). Nem domain-ontológia.

## DRIFT  (memória vs kód eltérés — ember dönti el)
- (nincs) — a legkockázatosabb promotálandó állítást (grounding-fotó kicsinyítés) a kódban ellenőriztem: `src/generator/images.ts:130` a `shrinkForVision`-t CSAK `raw.length > MAX_INLINE_BYTES` (3 MB) ágon hívja, a `VISION_MAX_EDGE=1568` alapból nem érinti a normál grounding-fotókat → a memória „a sharp csak a >3 MB képet menti / a pixelek maradnak" állítása PONTOS.

## ARCHIVE  (teljesen fölöslegessé vált memóriák)
- (nincs) — a promotált magok többsége nagyobb, élő projekt-memóriák RÉSZE (domain-automation, ai-cost-meter, offer-mandate), amelyek epizodikus státusz-tartalmat is hordoznak (nyitott pontok, élesítés-állapot) → a forrás-memóriák maradjanak; csak a tartós mag került az ontológiába.
